import React from 'react';
import classes from './Languages.module.scss';

const languages = () => {
    return(
        <section className={`db-section ${classes.Languages}`}>languages section</section>
    )
}

export default languages;